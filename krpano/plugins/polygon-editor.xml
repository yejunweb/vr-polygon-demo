<krpano>
	<style name="poly_vertex"
		type="text"
		html=""
		css="background:transparent; padding:0; margin:0;"
		bg="false"
		padding="0"
		renderer="css3d"
		distorted="false"
		zoom="false"
		zorder="200"
		capture="true"
		enabled="true"
		handcursor="true"
		edge="center"
		vcenter="true"
		ondown="drag_poly_vertex();"
		onup="js(onVertexDragEnd());"
	/>

	<style name="poly_act_btn"
		type="text"
		html=""
		css="background:transparent; padding:0; margin:0;"
		bg="false"
		padding="0"
		renderer="css3d"
		distorted="false"
		zoom="false"
		zorder="220"
		capture="true"
		enabled="true"
		handcursor="true"
		edge="bottom"
		oy="-18"
		vcenter="true"
	/>

	<action name="drag_poly_vertex">
		spheretoscreen(ath, atv, hotspotcenterx, hotspotcentery);
		sub(drag_adjustx, mouse.stagex, hotspotcenterx);
		sub(drag_adjusty, mouse.stagey, hotspotcentery);
		asyncloop(pressed,
			sub(dx, mouse.stagex, drag_adjustx);
			sub(dy, mouse.stagey, drag_adjusty);
			screentosphere(dx, dy, ath, atv);
			js(onVertexDrag(get(name)));
		);
	</action>

	<action name="drag_title">
		spheretoscreen(ath, atv, hotspotcenterx, hotspotcentery);
		sub(drag_adjustx, mouse.stagex, hotspotcenterx);
		sub(drag_adjusty, mouse.stagey, hotspotcentery);
		asyncloop(pressed,
			sub(dx, mouse.stagex, drag_adjustx);
			sub(dy, mouse.stagey, drag_adjusty);
			screentosphere(dx, dy, ath, atv);
			js(onTitleDrag(get(name)));
		);
	</action>

	<action name="drag_image">
		spheretoscreen(ath, atv, hotspotcenterx, hotspotcentery);
		sub(drag_adjustx, mouse.stagex, hotspotcenterx);
		sub(drag_adjusty, mouse.stagey, hotspotcentery);
		asyncloop(pressed,
			sub(dx, mouse.stagex, drag_adjustx);
			sub(dy, mouse.stagey, drag_adjusty);
			screentosphere(dx, dy, ath, atv);
			js(onImageDrag(get(name)));
		);
	</action>

	<events name="editor_draw_events" keep="true" enabled="false"
		onclick="js(onEditorPanoClick());"
	/>
</krpano>
